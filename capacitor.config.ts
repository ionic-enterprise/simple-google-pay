import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'simple-google-pay',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
